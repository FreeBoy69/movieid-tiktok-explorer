# TikTok Explorer listing.
# TikTokApi/Playwright is useful locally, but shared hosting should prefer yt-dlp.
TikTokApi>=7.0.0
yt-dlp>=2025.1.15
# Vibe Motion MP4/GIF rendering (also run: python -m playwright install --with-deps chromium).
playwright>=1.45
